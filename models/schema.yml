version: 2

models:
  - name: stg_fixtures
    description: Staging table for fixtures
    columns:
      - name: fixture_id
        description: Primary key
        tests:
          - unique
          - not_null